# Frontend env (Vite). Copy to `.env`.
# Leave EMPTY in dev → relative /api paths use the Vite proxy (same-origin), so the
# httpOnly refresh cookie works. Set to the backend URL only for a cross-origin prod build.
VITE_API_URL=
